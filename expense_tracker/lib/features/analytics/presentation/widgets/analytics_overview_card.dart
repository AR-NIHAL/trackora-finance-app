import 'package:flutter/material.dart';
import '../../../../shared/widgets/primary_card.dart';

class AnalyticsOverviewCard extends StatelessWidget {
  const AnalyticsOverviewCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const PrimaryCard(
      child: Text('Charts and analytics summary will appear here'),
    );
  }
}
